# Phase 2 Plan: `langgraph-ts-template` targeted refactor

## Goal

Remove `maw-cli` from the workflow template's dependency chain and shrink the scaffold contract to workflow-owned files only (`graph.ts` + `config.json`). Introduce the `prompts.global` / `prompts.agents` config shape, update the template engine and graph runtime to read from `maw.json` + the workflow-local `config.json`, and align all tests to the Phase 1 architecture.

## Scope

**`langgraph-ts-template` only — no changes to `maw-cli` or `maw-smoke`**

- `package.json` — remove `maw-cli` dep, `bin` field, `lint:langgraph-json` script
- `src/bin.ts` — delete
- `scripts/checkLanggraphPaths.js` — delete
- `src/scaffold/assets/config.json` — replace with new `prompts.global` / `prompts.agents` format
- `src/scaffold/assets/graph.ts.template` — add `__WORKFLOW_ID__` token
- `src/scaffold/assets/ov.conf` — delete (owned by `maw-cli`)
- `src/scaffold/assets/langgraph.json.template` — delete (generated by `maw-cli`)
- `src/scaffold/index.ts` — revise to `{ packageName, workflow }` + new `createScaffoldFiles()` + export `templateDir`
- `src/templates/defaults/general-coding.njk` — rename to `general.njk`
- `src/templates/defaults/project-context.njk` — delete (conflicts with OpenViking)
- `src/config.ts` — replace `MawConfig` with partial `WorkflowConfig` plus resolved defaults derived from the scaffold asset
- `src/templates/composition.ts` — resolved `TemplateComposition` shape for composed prompts; drop `AgentComposition`
- `src/templates/engine.ts` — new `CreateTemplateEngineOptions`, drop `git` source, simplify to custom-then-embedded
- `src/agent/graph.ts` — new `GraphConfig`, rewritten `loadRuntime` reading `maw.json` + workflow config
- `src/index.ts` — update re-exports
- `tests/unit/package-metadata.spec.ts` — remove `bin.ts` read and `maw-cli` proxy test
- `tests/unit/scaffold.spec.ts` — rewrite for new contract
- `tests/unit/config.spec.ts` — rewrite for `WorkflowConfig`
- `tests/unit/templates.spec.ts` — update for the new engine options, drop git source coverage, keep custom `workspacePath` coverage
- `tests/unit/public-api.spec.ts` — update `createGraph()` coverage if the new `GraphConfig` surface requires it
- `tests/integration/scaffold.test.ts` — rewrite for new scaffold contract
- `tests/integration/graph.test.ts` — update config shapes and prompt assertions

## Out of Scope

- `maw-cli` — no changes; Phase 1 is complete
- `maw-smoke` — no changes; smoke tests already exercise the Phase 1 contract
- `src/openviking/` — Phase 4 concern
- Prompt management commands (`prompt:list`, `prompt:preview`) — Phase 3
- Model provider wiring, LLM tool loop — Phase 5
- Git-backed template repos (`.maw/template-repos/`) — dropped; can be re-added post-MVP

## Decisions Cleared

- Generated target-project `graph.ts` must call `createGraph({ workflow: '__WORKFLOW_ID__' })`. The Phase 0 init plan example is updated in this revision to match.
- `src/scaffold/assets/config.json` is the single source of truth for embedded workflow prompt defaults. `DEFAULT_WORKFLOW_CONFIG` must be derived from that asset instead of duplicating the literal defaults in code.
- `loadProjectConfig()` falls back to `DEFAULT_PROJECT_CONFIG` only when `maw.json` is missing. If `maw.json` exists but cannot be read, parsed, or validated, the workflow runtime must throw.
- `WorkflowConfig` is optional and partial. Missing keys inherit defaults via deep merge.
- Explicit empty arrays in workflow prompt config also inherit defaults. Example: `prompts.global: []` keeps the embedded global defaults instead of clearing them.
- Invalid workflow-local prompt config or missing configured snippets must emit a loud warning and fall back to embedded defaults instead of breaking the workflow.

## Execution Notes

### `WORKFLOW_ID` derivation
Derive `WORKFLOW_ID` from `WORKFLOW_PACKAGE_NAME` by stripping any `@scope/` prefix:
```ts
const WORKFLOW_ID = WORKFLOW_PACKAGE_NAME.replace(/^@[^/]+\//, '');
```
For `langgraph-ts-template` this produces `langgraph-ts-template`. For `@org/coding` it produces `coding`. Template forks inherit the correct ID automatically when they rename their package.

### New scaffold contract
`maw-cli init` validates exactly these fields from a package's `./scaffold` export:
- `scaffold.packageName: string`
- `scaffold.workflow: string`
- `createScaffoldFiles: () => { 'graph.ts': string; 'config.json': string }`

Remove `directories`, `gitignore`, `rules`, and `assets` from the scaffold object entirely — `maw-cli` no longer reads them. The old types `ScaffoldAsset`, `ScaffoldRules`, `MawScaffold`, and the constants `SCAFFOLD_DIRECTORIES`, `SCAFFOLD_GITIGNORE`, `SCAFFOLD_RULES` should be deleted.

`readScaffoldAsset()` may remain only if it is still needed internally to derive `DEFAULT_WORKFLOW_CONFIG` from the scaffold asset. It is not part of the `maw-cli init` handoff contract and should not drag the old scaffold surface forward.

### `graph.ts.template` dual-token substitution
Add a second token `__WORKFLOW_ID__` alongside the existing `__WORKFLOW_PACKAGE__`. Both are substituted in `createScaffoldFiles()`. The generated target-project file must become:
```ts
import { createGraph } from 'coding';
export const graph = createGraph({ workflow: 'coding' });
```
`__WORKFLOW_PACKAGE__` → package name (the import), `__WORKFLOW_ID__` → workflow id (the runtime config path lookup). `createScaffoldFiles()` takes no arguments and reads `WORKFLOW_PACKAGE_NAME` / `WORKFLOW_ID` directly.

### `project-context.njk` deletion and `general-coding.njk` rename
`project-context.njk` (`Workspace path: {{ workspacePath }}`) is deleted. OpenViking owns project context retrieval; a static workspace-path injection conflicts with that role.

`general-coding.njk` is renamed to `general.njk` to match the snippet name declared in the new `config.json` format (`"general"`). Any test that previously referenced `'general-coding'` as a snippet name must be updated to `'general'`. Any test that previously referenced `readScaffoldAsset('ov')` or assertions about `project-context` content must be removed.

### `WorkflowConfig` shape
```ts
// src/config.ts
export type WorkflowConfig = {
    prompts?: {
        global?: readonly string[];
        agents?: Readonly<Record<string, readonly string[]>>;
    };
};

export type ResolvedWorkflowConfig = {
    prompts: {
        global: readonly string[];
        agents: Readonly<Record<string, readonly string[]>>;
    };
};
```
`WorkflowConfig` is the on-disk or caller-provided override bag. Empty objects and partial objects are valid. `ResolvedWorkflowConfig` is the normalized form used by the runtime after defaults are applied.

Merge rules:
- Missing keys inherit defaults.
- Explicit empty arrays also inherit defaults.
- Non-empty arrays replace the inherited value for that slot.
- Unspecified agents inherit their default arrays unchanged.

Composed order remains `[...global, ...agents[agent]]`.

### `DEFAULT_WORKFLOW_CONFIG`
Replaces the old `createConfig()`. Do not hand-write the literal defaults in `config.ts`. Parse `src/scaffold/assets/config.json` and export the resolved result as `DEFAULT_WORKFLOW_CONFIG` so the scaffolded file contents and embedded runtime defaults cannot drift.

### `TemplateComposition` in `composition.ts`
The template engine should operate on resolved prompt data only. `TemplateComposition` stays the normalized `{ global, agents }` shape:
```ts
export interface TemplateComposition {
    readonly global: readonly string[];
    readonly agents: Readonly<Record<string, readonly string[]>>;
}

export const resolveSnippets = (cfg: TemplateComposition, agent: string): string[] => {
    const agentSnippets = cfg.agents[agent];
    if (!agentSnippets) throw new Error(`Unknown prompt agent: ${agent}`);
    return [...cfg.global, ...agentSnippets];
};
```
Remove `AgentComposition` — per-agent entries are now plain `string[]`, not wrapper objects. The deep merge from partial `WorkflowConfig` into resolved `TemplateComposition` belongs in `config.ts` / `graph.ts`, not in `composition.ts`.

### Config split: exported vs internal
- `WorkflowConfig`, `ResolvedWorkflowConfig`, `workflowConfigSchema`, `parseWorkflowConfig`, `loadWorkflowConfig`, `resolveWorkflowConfig`, and `DEFAULT_WORKFLOW_CONFIG` — exported from `./config`; this is the only workflow-owned config schema
- Project config (`maw.json` shape) — internal to `graph.ts` only; never exported from the package; `maw.json` is `maw-cli` territory

### Runtime config loading in `graph.ts`
`loadRuntime(cfg)` must follow this chain:

1. **Project config**: try to read `maw.json` from `root`.
   - Missing file → use `{ workspace: '.', templates: { customPath: '.maw/templates' } }`
   - Existing but unreadable / invalid file → throw
2. **Workflow config** (priority order):
   - If `cfg.workflowConfig` is provided → deep-merge it into `DEFAULT_WORKFLOW_CONFIG` with `resolveWorkflowConfig()` (no I/O)
   - Else if `cfg.workflow` is provided → try `root/.maw/graphs/<workflow>/config.json`
     - Missing file → `DEFAULT_WORKFLOW_CONFIG`
     - Existing but invalid file → loud warning + `DEFAULT_WORKFLOW_CONFIG`
     - Valid file → deep-merge it into `DEFAULT_WORKFLOW_CONFIG`
   - Else → `DEFAULT_WORKFLOW_CONFIG`
3. **Agent selection**: `cfg.agent ?? first key of resolvedWorkflowConfig.prompts.agents`
4. **Prompt composition fallback**:
   - Try the resolved workflow config first
   - If composition fails because a configured snippet cannot be resolved, emit a loud warning and retry once with `DEFAULT_WORKFLOW_CONFIG`
   - If the embedded default snippets fail, rethrow; that is a package bug, not user config

`loadProjectConfig` should be a plain internal `async` function in `graph.ts` — not exported.

### Template engine `sources` simplification
The old `config.templates.sources` array is gone. The new engine always:
1. Checks `customPath` resolved against `root` — silently skips if the directory does not exist
2. Appends `templateDir` (embedded defaults) — always last, always present

Delete `repoDirs()` and the `'git'` branch entirely. Drop the `strict` option from `CreateTemplateEngineOptions`.

### `workspace` in the engine
`project-context.njk` is deleted, so no default template renders `{{ workspacePath }}`. Keep `workspace?: string` in `CreateTemplateEngineOptions` as a forward-compat default for custom templates that may reference `{{ workspacePath }}`. Set the bag entry to `opts.workspace ?? ''`. `graph.ts` passes `workspace` from the loaded project config. If a user-defined `.maw/templates/my-snippet.njk` uses `{{ workspacePath }}`, it will resolve correctly without additional wiring.

### `templateDir` export from scaffold
Export `templateDir` from `src/scaffold/index.ts` using the same dual-path pattern as `defaultDir()` in `engine.ts`:
```ts
const resolveTemplateDir = (): string => {
    const local = fileURLToPath(new URL('../templates/defaults', import.meta.url));
    if (existsSync(local)) return local;
    return fileURLToPath(new URL('../../src/templates/defaults', import.meta.url));
};
export const templateDir = resolveTemplateDir();
```
This is the path `maw-cli prompt:preview` (Phase 3) will use to locate embedded snippets.

### `graph.name` assignment
`cfg.config?.graph.name` no longer exists. New assignment:
```ts
graph.name = cfg.name ?? cfg.workflow ?? DEFAULT_GRAPH_NAME;
```

### `package-metadata.spec.ts` risks
- Line 9 executes `readFileSync(resolve(root, 'src/bin.ts'), 'utf8')` **at module-load time** — before any `it()` runs. Deleting `bin.ts` without updating this file causes every test in the file to fail at import. This update must be in the same step as `bin.ts` deletion.
- When `templateDir` becomes part of the scaffold contract, `package-metadata.spec.ts` must also keep asserting that `src/templates/defaults` stays in `package.json.files`. Phase 3 `prompt:preview` depends on those embedded templates being published.

### `lint:langgraph-json` + `lint:all`
`lint:langgraph-json` invokes `scripts/checkLanggraphPaths.js`, which validates the now-retired root-level `langgraph.json`. Both the script file and the npm script must be removed. Update `lint:all` to:
```json
"lint:all": "bun run lint && bun run format:check"
```

### `resolveEnvVars` removal rationale
The new workflow-local `config.json` contains only static snippet names — no `${VAR}` placeholders. `maw.json` also forbids interpolation per the init plan. `resolveEnvVars`, `envPattern`, `resolveValue`, and `hostEnv` have no remaining callers and should be deleted.

## Work Plan

### 1. Remove maw-cli entanglement

Couples `bin.ts` deletion with the only test that reads `bin.ts` at import time.

- [x] `package.json`: remove `maw-cli` from `dependencies`
- [x] `package.json`: remove the `bin` field (`"langgraph-ts-template": "./dist/bin.js"`)
- [x] `package.json`: remove the `lint:langgraph-json` script
- [x] `package.json`: update `lint:all` to `"bun run lint && bun run format:check"`
- [x] Delete `src/bin.ts`
- [x] Delete `scripts/checkLanggraphPaths.js`
- [x] `tests/unit/package-metadata.spec.ts`:
  - Remove the `const bin = readFileSync(resolve(root, 'src/bin.ts'), 'utf8')` line
  - Delete the entire `'depends on and proxies to maw-cli'` test block

Verify:
- [x] `bun run test -- tests/unit/package-metadata.spec.ts`

### 2. Normalize embedded snippet names and remove `project-context`

Keep this step limited to template assets and the tests that directly depend on them. Do not pull the scaffold-contract rewrite into this step.

- [x] Delete `src/templates/defaults/project-context.njk`
- [x] Rename `src/templates/defaults/general-coding.njk` → `src/templates/defaults/general.njk`
- [x] Update still-current references to the old snippet names while preserving the legacy config/runtime API for this step:
  - `src/scaffold/assets/config.json`
  - `tests/unit/templates.spec.ts`
  - `tests/integration/graph.test.ts`
- [x] `tests/unit/templates.spec.ts`:
  - [x] Replace `'general-coding'` references with `'general'`
  - [x] Remove assertions that depended on embedded `project-context`
  - [x] Add or keep coverage proving a custom `.maw/templates/*.njk` snippet can still render `{{ workspacePath }}` when `workspace` is provided by the caller
- [x] `tests/integration/graph.test.ts`:
  - [x] Update snippet names to `'general'`
  - [x] Remove `Workspace path:` ordering assertions that depended on the deleted embedded snippet

Verify:
- [x] `bun run test -- tests/unit/templates.spec.ts`
- [x] `bun run test:int -- tests/integration/graph.test.ts`

### 3. Shrink the scaffold contract and publish template metadata

This step changes the `./scaffold` handoff surface only. Do not mix the runtime config rewrite into it.

- [x] Delete `src/scaffold/assets/ov.conf`
- [x] Delete `src/scaffold/assets/langgraph.json.template`
- [x] `src/scaffold/assets/graph.ts.template`: add `__WORKFLOW_ID__` token:
  ```ts
  import { createGraph } from '__WORKFLOW_PACKAGE__';
  export const graph = createGraph({ workflow: '__WORKFLOW_ID__' });
  ```
- [x] `src/scaffold/index.ts`:
  - Remove `assetFiles.ov` and `assetFiles.langgraph` keys and their `resolveAsset` calls
  - Remove types: `ScaffoldAsset`, `ScaffoldRules`, `MawScaffold`
  - Remove exports: `SCAFFOLD_DIRECTORIES`, `SCAFFOLD_GITIGNORE`, `SCAFFOLD_RULES`
  - Keep `WORKFLOW_PACKAGE_NAME`
  - Add `WORKFLOW_ID = WORKFLOW_PACKAGE_NAME.replace(/^@[^/]+\//, '')`
  - Add `templateDir` export (see Execution Notes — `resolveTemplateDir` pattern)
  - Revise `scaffold` object and type to `{ packageName: string; workflow: string }` only
  - Revise `createScaffoldFiles()`: no parameters; substitute both `__WORKFLOW_PACKAGE__` and `__WORKFLOW_ID__` tokens; return `Record<'graph.ts' | 'config.json', string>` using only the `config` and `graph` assets
  - Update `ScaffoldAssetName` to `'config' | 'graph'` only (remove `'ov'` and `'langgraph'`)
  - Keep `readScaffoldAsset()` only if it is still required internally by `src/config.ts`
- [x] `tests/unit/scaffold.spec.ts`: rewrite:
  - Assert `scaffold.packageName` is a non-empty string
  - Assert `scaffold.workflow` is a non-empty string
  - Add direct coverage that scoped package names strip their scope when deriving `WORKFLOW_ID` (extract a tiny local helper only if needed for testability)
  - Assert `createScaffoldFiles()` (no args) returns an object with exactly `'graph.ts'` and `'config.json'` as keys
  - Assert `files['graph.ts']` contains `createGraph({ workflow:` and the package name in the import
  - Assert `files['config.json']` parses as valid JSON; final prompt-shape assertions move to Step 4 when the runtime config model changes
  - Assert `templateDir` resolves to an existing directory containing `.njk` files
  - Remove all tests for: `scaffold.directories`, `scaffold.gitignore`, `scaffold.rules`, `scaffold.assets`, `readScaffoldAsset('ov')`, `readScaffoldAsset('langgraph')`, `langgraph.json`, `.maw/config.json`, `ov.conf`
- [x] `tests/integration/scaffold.test.ts`: rewrite:
  - Import `scaffold`, `createScaffoldFiles`, `templateDir` from `'../../src/scaffold/index.js'`
  - Assert `scaffold.workflow` is a non-empty string
  - Assert `createScaffoldFiles()` returns an object with exactly the keys `'graph.ts'` and `'config.json'`
  - Write those two files into a temp dir and assert the graph file contains `createGraph({ workflow:` and an import of the package name
  - Assert `files['config.json']` parses as valid JSON
  - Assert idempotency: running `createScaffoldFiles()` twice returns identical content
  - Assert `templateDir` resolves to an existing directory that contains at least one `.njk` file
  - Remove all assertions for `directories`, `gitignore`, `rules`, `assets`, `maw-cli` handoff fields
  - Remove the `scripts/checkLanggraphPaths.js` shell invocation
- [x] `tests/unit/package-metadata.spec.ts`:
  - Keep asserting `src/scaffold/assets` is published
  - Add or keep assertion that `src/templates/defaults` remains published because `templateDir` depends on it

Verify:
- [x] `bun run test -- tests/unit/package-metadata.spec.ts tests/unit/scaffold.spec.ts`
- [x] `bun run test:int -- tests/integration/scaffold.test.ts`

### 4. Rewrite the workflow prompt config and runtime stack

This is the smallest safe runtime slice. `src/config.ts`, `src/templates/composition.ts`, `src/templates/engine.ts`, `src/agent/graph.ts`, and the related unit tests are currently coupled and must move together.

- [x] `src/scaffold/assets/config.json`: replace with the finalized prompt-only shape from `init-plan.md`:
  ```json
  {
      "prompts": {
          "global": ["general", "security"],
          "agents": {
              "planner": ["research-rules"],
              "coder": ["typescript"]
          }
      }
  }
  ```

- [x] `src/config.ts`:
  - Remove: `mawConfigSchema`, `MawConfig`, `DEFAULT_CONFIG_PATH`, `createConfig`, `parseConfig`, `loadConfig`, `resolveEnvVars`, `envPattern`, `resolveValue`, `hostEnv`, `agentSchema`
  - Add partial `WorkflowConfig` and resolved `ResolvedWorkflowConfig` (see Execution Notes)
  - Add `workflowConfigSchema` (Zod): validates the optional/partial `prompts` shape
  - Add `parseWorkflowConfig(value: unknown): WorkflowConfig`
  - Add `loadWorkflowConfig(path: string): Promise<WorkflowConfig>` — reads the file, parses JSON, runs `parseWorkflowConfig`
  - Add `resolveWorkflowConfig(value?: WorkflowConfig): ResolvedWorkflowConfig` — deep-merges partial values into defaults
  - Add `DEFAULT_WORKFLOW_CONFIG` by parsing `src/scaffold/assets/config.json`; do not duplicate the literal defaults inline
  - Implement merge semantics from the resolved decisions section: missing keys and explicit empty arrays inherit defaults

- [x] `src/templates/composition.ts`:
  - Replace `AgentComposition` and `TemplateComposition` with the resolved `TemplateComposition` interface: `{ global: readonly string[]; agents: Record<string, readonly string[]> }`
  - Remove `AgentComposition` interface entirely — per-agent entries are plain `string[]`
  - Update `resolveSnippets(cfg: TemplateComposition, agent: string)` to return `[...cfg.global, ...agentSnippets]`

- [x] `src/templates/engine.ts`:
  - Replace `CreateTemplateEngineOptions.config: MawConfig` with:
    - `prompts: TemplateComposition`
    - `workspace?: string` (defaults `workspacePath` bag entry to `''`)
    - `customPath?: string` (defaults to `'.maw/templates'`)
    - `root?: string` (defaults to `process.cwd()`)
  - Remove the `strict` option
  - Remove `repoDirs()` function and the `'git'` source branch from `sources()`
  - Simplify `sources()`: try `customPath` resolved against `root` (skip if directory missing); always append `templateDir` (embedded)
  - Update `compose()`: call `resolveSnippets(opts.prompts, agent)`; set `workspacePath: opts.workspace ?? ''` in the bag
  - Import `TemplateComposition` from `'./composition.js'`; remove `MawConfig` import

- [x] `src/agent/graph.ts`:
  - Replace `config?: MawConfig` with `workflowConfig?: WorkflowConfig` in `GraphConfig`
  - Replace `configPath?: string` with `workflow?: string` in `GraphConfig`
  - Add internal `ProjectConfig` interface: `{ workspace: string; templates: { customPath: string } }`
  - Add internal `DEFAULT_PROJECT_CONFIG` constant: `{ workspace: '.', templates: { customPath: '.maw/templates' } }`
  - Add internal `loadProjectConfig(root: string): Promise<ProjectConfig>` — reads `maw.json`; falls back only when the file is missing and throws when an existing file is invalid
  - Rewrite `loadRuntime(cfg)` per the chain in Execution Notes; return the resolved workflow config, project config, root, and selected agent
  - Update the engine creation in `prompt()` to pass `prompts: resolvedWorkflowConfig.prompts`, `workspace: projectConfig.workspace`, and `customPath: projectConfig.templates.customPath`
  - Catch invalid on-disk workflow config loads, emit a loud warning, and fall back to `DEFAULT_WORKFLOW_CONFIG`
  - Catch prompt-composition failures caused by configured snippet names, emit a loud warning, and retry once with `DEFAULT_WORKFLOW_CONFIG`
  - Update `graph.name` assignment to `cfg.name ?? cfg.workflow ?? DEFAULT_GRAPH_NAME`

- [x] `src/index.ts`:
  - Remove from config re-exports: `DEFAULT_CONFIG_PATH`, `createConfig`, `loadConfig`, `mawConfigSchema`, `parseConfig`, `resolveEnvVars`, `MawConfig`
  - Remove from scaffold re-exports: `SCAFFOLD_DIRECTORIES`, `SCAFFOLD_GITIGNORE`, `SCAFFOLD_RULES`, `MawScaffold`, `ScaffoldAsset`, `ScaffoldAssetName`, `ScaffoldRules`, `readScaffoldAsset`
  - Add to config re-exports: `WorkflowConfig`, `ResolvedWorkflowConfig`, `workflowConfigSchema`, `parseWorkflowConfig`, `loadWorkflowConfig`, `resolveWorkflowConfig`, `DEFAULT_WORKFLOW_CONFIG`
  - Add to scaffold re-exports: `WORKFLOW_ID`, `templateDir`

- [x] `tests/unit/config.spec.ts`: rewrite:
  - Test `workflowConfigSchema.parse` accepts valid partial `WorkflowConfig` objects, including `{}`
  - Test `parseWorkflowConfig` throws on invalid input (wrong types, non-string entries, malformed nested values)
  - Test `loadWorkflowConfig` reads and returns valid partial config from a temp JSON file
  - Test `resolveWorkflowConfig` deep-merges partial overrides into defaults
  - Test explicit empty arrays inherit defaults instead of clearing them
  - Test `DEFAULT_WORKFLOW_CONFIG.prompts.global` is a non-empty array
  - Test `DEFAULT_WORKFLOW_CONFIG.prompts.agents` has `planner` and `coder` entries as non-empty arrays
  - Remove all tests for `createConfig`, `loadConfig`, `resolveEnvVars`

- [x] `tests/unit/templates.spec.ts`: update:
  - Replace `makeConfig()` with resolved prompt fixtures for the new engine options shape
  - Keep using snippet names that exist in `src/templates/defaults/` (`'general'`, `'security'`, `'research-rules'`, `'typescript'`) to avoid resolution failures
  - Update all `createTemplateEngine()` calls to `{ prompts: ..., root }`
  - Remove the test exercising `.maw/template-repos/` (git source dropped)
  - Replace the "enabled source directory is missing" strict-fail test with: custom path absent → engine composes using embedded templates only (no error)
  - Update the "source precedence" test: custom template overrides embedded for the same snippet name (no git repo); verify `prompts.global` snippets appear before agent-specific ones
  - Keep or adapt the custom `workspacePath` coverage added in Step 2 so custom templates still render it correctly
  - Update the "unknown agent" and "missing snippet" tests to use the new options shape

- [x] `tests/unit/scaffold.spec.ts`: update final `config.json` assertions to the prompt-only shape:
  - `prompts.global` is a non-empty array
  - `prompts.agents.planner` and `prompts.agents.coder` are non-empty arrays

- [x] `tests/unit/public-api.spec.ts`: verify `createGraph({ name: 'Docs Agent' })` still compiles and `app.invoke` succeeds; adjust only if the new `GraphConfig` signature causes a type error

Verify:
- [x] `bun run typecheck`
- [x] `bun run build`
- [x] `bun run test -- tests/unit/config.spec.ts tests/unit/templates.spec.ts tests/unit/scaffold.spec.ts tests/unit/public-api.spec.ts`

### 5. Update integration coverage for the final runtime contract

These tests touch the filesystem and require the built source, so they run in the `test:int` mode separately from unit tests after Step 4 lands.

- [x] `tests/integration/scaffold.test.ts`: finish the final assertions for the new scaffold payload:
  - [x] Assert `files['config.json']` parses with a non-empty `prompts.global` array and `prompts.agents` record
  - [x] Keep the idempotency and `templateDir` assertions from Step 3

- [x] `tests/integration/graph.test.ts`: update:
  - [x] Replace `makeConfig()` with partial `WorkflowConfig` fixtures
  - [x] First test: pass `workflowConfig` to `createGraph()` and assert partial overrides merge with defaults instead of replacing them wholesale
  - [x] Update the prompt-ordering assertion to reflect the final composition: resolved global snippets first, then agent-specific
  - [x] Disk-loading happy-path test:
     - [x] Write `maw.json` to `root` with `{ workspace: '.', openviking: { enabled: false, host: 'localhost', port: 1933 }, templates: { customPath: '.maw/templates' } }`
     - [x] Create `root/.maw/graphs/test-workflow/` and write `config.json` with a `WorkflowConfig` in the new shape
     - [x] Call `createGraph({ root, workflow: 'test-workflow' })`
     - [x] Assert the composed prompt contains the coder agent's TypeScript skill content
  - [x] Add a missing-workflow-config test: `createGraph({ root, workflow: 'missing' })` falls back to embedded defaults
  - [x] Add an invalid-workflow-config test: malformed `.maw/graphs/<workflow>/config.json` triggers a loud warning and falls back to embedded defaults
  - [x] Add a missing-snippet test: valid workflow config that references a missing snippet name triggers a loud warning and falls back to embedded defaults
  - [x] Add an invalid-`maw.json` test: malformed existing `maw.json` rejects instead of silently defaulting

Verify:
- [x] `bun run test:int`

## Verification

### Per-step verification

- [x] Step 1: `bun run test -- tests/unit/package-metadata.spec.ts`
- [x] Step 2: `bun run test -- tests/unit/templates.spec.ts`
- [x] Step 2: `bun run test:int -- tests/integration/graph.test.ts`
- [x] Step 3: `bun run test -- tests/unit/package-metadata.spec.ts tests/unit/scaffold.spec.ts`
- [x] Step 3: `bun run test:int -- tests/integration/scaffold.test.ts`
- [x] Step 4: `bun run typecheck`
- [x] Step 4: `bun run build`
- [x] Step 4: `bun run test -- tests/unit/config.spec.ts tests/unit/templates.spec.ts tests/unit/scaffold.spec.ts tests/unit/public-api.spec.ts`
- [x] Step 5: `bun run test:int`

### Phase completion

- [x] `bun run typecheck` in `langgraph-ts-template`
- [x] `bun run build` in `langgraph-ts-template`
- [x] `bun run lint` in `langgraph-ts-template`
- [x] `bun run test` in `langgraph-ts-template`
- [x] `bun run test:int` in `langgraph-ts-template`

## Exit Criteria

- [x] `langgraph-ts-template/package.json` has no `maw-cli` in `dependencies`, no `bin` field, no `lint:langgraph-json` script
- [x] `src/bin.ts` and `scripts/checkLanggraphPaths.js` do not exist
- [x] `src/scaffold/assets/` contains only `config.json` and `graph.ts.template`; `ov.conf` and `langgraph.json.template` are gone
- [x] `src/templates/defaults/project-context.njk` does not exist; `src/templates/defaults/general.njk` exists (renamed from `general-coding.njk`)
- [x] `scaffold.workflow` is a non-empty string; `scaffold.directories`, `scaffold.gitignore`, `scaffold.rules`, `scaffold.assets` do not exist on the object
- [x] `createScaffoldFiles()` (no args) returns exactly `{ 'graph.ts': string, 'config.json': string }`; the `graph.ts` value contains `createGraph({ workflow: '...' })`; the `config.json` value parses with `prompts.global` and `prompts.agents`
- [x] `templateDir` is exported from `./scaffold` and resolves to the embedded templates directory
- [x] `./config` export exposes `WorkflowConfig`, `ResolvedWorkflowConfig`, `workflowConfigSchema`, `parseWorkflowConfig`, `loadWorkflowConfig`, `resolveWorkflowConfig`, and `DEFAULT_WORKFLOW_CONFIG`; `MawConfig`, `resolveEnvVars`, and all old symbols are absent
- [x] `DEFAULT_WORKFLOW_CONFIG` is derived from the scaffold asset, not duplicated inline in code
- [x] `WorkflowConfig` accepts partial prompt overrides; missing keys and explicit empty arrays inherit the embedded defaults
- [x] `createGraph({ workflow: 'test' })` constructs the config path as `.maw/graphs/test/config.json` from `process.cwd()`; falls back to `DEFAULT_WORKFLOW_CONFIG` when the file is missing or invalid, and throws when an existing `maw.json` is invalid
- [x] `bun run build && bun run lint && bun run test && bun run test:int` all pass in `langgraph-ts-template`
