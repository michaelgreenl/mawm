{
  "name": "coding",
  "module": "src/graph/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist",
    "build": "bun scripts/build.js",
    "build:mawm": "bun clean && bun run build && mawm update -g coding",
    "format": "prettier --write src",
    "format:check": "prettier --check src",
    "lint": "eslint src",
    "lint:all": "bun lint && bun format:check"
  },
  "dependencies": {
    "@langchain/core": "^1.1.45",
    "@langchain/langgraph": "^1.3.0",
    "@opencode-ai/sdk": "^1.15.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "prettier": "^3.5.3",
    "typescript": "^5",
    "typescript-eslint": "^8.32.0"
  }
}
