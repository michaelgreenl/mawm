// Scaffold placeholder for the workflow-local secret guard runtime plugin.
// The workflow build owns this asset; CLI bundling should only copy it downstream.
// Runtime implementation must block agent/tool secret reads while preserving the CLI-owned OV config resolution exception.
