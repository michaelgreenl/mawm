# Seeded by @mawm/cli.
# Project-scoped OpenViking server config placeholders are resolved from process env first,
# then from the target project's `.env` for still-missing values when `mawm ov server` runs.
