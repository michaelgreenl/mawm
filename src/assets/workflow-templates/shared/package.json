{
  "name": "workflow-template",
  "module": "src/graph/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit --module preserve --moduleResolution bundler --target esnext src/graph/index.ts",
    "test": "vitest run",
    "build": "bun scripts/build.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@langchain/langgraph": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5",
    "vitest": "^4.1.7"
  }
}
