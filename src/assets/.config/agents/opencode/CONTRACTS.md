These assets get copied into user's `~/.config/opencode/` when installing `mawm` globally

Non-destructive by default

If there's existing assets or opencode.json, install only copies/writes missing assets/config
